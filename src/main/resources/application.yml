spring:
  profiles:
    active: oauth2
  application:
    name: likelion-security
  security:
    user:
      password: likelion
      name: likelion
      roles: USER

server:
  port: 8085
  servlet:
    session:
      timeout: 10m
      cookie:
        name: JSESSIONID


secret-key: likelion-backend-plus-4th-process-1st-project-secretkey
logging:
  level:
    org.springframework.security: DEBUG

